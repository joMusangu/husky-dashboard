flask==3.0.0
flask-cors==4.0.0
numpy==1.24.3
pandas==2.0.3
matplotlib==3.7.2

# ROS bag parsing (optional - install if you have ROS bag files)
# rosbag  # For ROS1 - install via: pip install rosbag
# rosbags  # For ROS2 - install via: pip install rosbags

# CAN bus parsing (optional - install if you have CAN log files)
# python-can  # Install via: pip install python-can

